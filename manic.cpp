#include <cmath>
#include <iostream>
#include <vector>
#include "Core.h"

using namespace std;

int main()
{
    vector <Point> vertex; // here only for 2D (just for now..)
    for(size_t i = 0; i < vertex.size(); ++i)
    {

    }

    return 0;
}
